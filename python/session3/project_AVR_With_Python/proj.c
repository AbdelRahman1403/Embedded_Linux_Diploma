#include <iostream>
             using namespace std
             int set_bits(){
                int bits[8];
                for(int i = 0 ; i < 8 ; i++)
                {
                 cout << "Please enter bit" << i << "mode" :;
                 cin >> bits[i];
                 }                